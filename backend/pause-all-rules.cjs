const axios=require("axios");(async()=>{const l=await axios.post("http://localhost:3000/api/login",{password:"pass55184"});const c=l.headers["set-cookie"];const rules=await axios.get("http://localhost:3000/api/reposter/rules",{headers:{Cookie:c}});let paused=0;for(const rule of rules.data){if(rule.isActive!==false){rule.isActive=false;await axios.patch("http://localhost:3000/api/reposter/rules/"+rule.id,rule,{headers:{Cookie:c}});console.log("? Paused:",rule.name);paused++;}}console.log("\n? Total paused:",paused);process.exit();})();
