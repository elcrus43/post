const axios=require("axios");(async()=>{const l=await axios.post("http://localhost:3000/api/login",{password:"pass55184"});const c=l.headers["set-cookie"];const rules=await axios.get("http://localhost:3000/api/reposter/rules",{headers:{Cookie:c}});let resumed=0;for(const rule of rules.data){if(rule.isActive===false){rule.isActive=true;await axios.patch("http://localhost:3000/api/reposter/rules/"+rule.id,rule,{headers:{Cookie:c}});console.log("? Resumed:",rule.name);resumed++;}}console.log("\n? Total resumed:",resumed);process.exit();})();
